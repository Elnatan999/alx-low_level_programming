#include <stdio.h>
#include "holberton.h"
/**
 * times_table - prints the times table
 * Return: 0
 */
void times_table(void)
{
  int c = 0;
  do{
    c = 0 * c;
    _putchar(c);
    c++;
  } while (c <= 10);
}
