#include <stdio.h>
/**
 * main - hexadecimal
 * Return: 0
 */
int main(void)
{
  int ch;
  char c;
  for (ch = 0; ch < 10; ch++)
    putchar(ch);
  for (c = 'a'; c < 'g'; c++)
    putchar(c);
  putchar('\n');
  return (0);
}
