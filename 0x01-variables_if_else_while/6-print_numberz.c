#include <stdio.h>
/**
 * main - prints numbers
 * Return: 0
 */
int main(void)
{
long ch;
long x;
for (ch = 0; ch < 10; ch++)
if (ch > 10)
x = ch / 10;
putchar(x % 10 + '0');
putchar(x);
/**
 * main - printing number
 * Return: 0
 */
return (0);
}
