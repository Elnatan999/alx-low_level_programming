#include <stdio.h>
/**
 * main - prints alphabets
 * Return= 0
 */
int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
putchar('\n');
/**
 * main - prints alphabets
 * Return= 0 
 */
return (0);
}
