#include <stdio.h>
/**
 *  main - prints alphabet
 * Return:0
 */
int main(void)
{
for ( ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
for ( ch = 'A'; ch <= 'Z'; ch++)
putchar(ch);
putchar('\n');
/**
 *  main - prints alphabet
 * Return: 0
 */
return (0);
}
